import * as React from 'react';

export class App extends React.Component<undefined, undefined> {
  render() {
    return (
      <div>
        <h2>Welcome to Electron React with Typescript!</h2>
      </div>
    );
  }
}
